# Mix Up

Personal App used to randomize choices.

## Setup

```bash
yarn install
```

## Commands

- **`yarn start`** — start dev server (Vite)
- **`yarn test`** — run tests
- **`yarn lint`** — runs lint
